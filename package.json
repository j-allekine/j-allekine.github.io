{
  "name": "j-allekine.github.io",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "astro check",
    "test": "npm run test:acceptance",
    "test:acceptance": "npm run build && playwright test --grep @unconfigured --workers=1 && astro build --mode test && playwright test --grep-invert @unconfigured --workers=1"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.3.0",
    "@fontsource-variable/geist-mono": "^5.3.0",
    "@phosphor-icons/web": "^2.1.2",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3"
  }
}
