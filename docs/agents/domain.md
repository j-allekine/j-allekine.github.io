# Domain documentation

This is a single-context repository.

## Before working

Read:

- `CONTEXT.md`
- relevant architecture decisions under `docs/adr/`

Proceed silently when no relevant architecture decision exists.

## Vocabulary

Use the canonical terms defined in `CONTEXT.md`. Avoid synonyms that the glossary explicitly rejects.

If proposed work conflicts with an existing architecture decision, report the conflict instead of silently overriding it.
